MAX GROUP OF COMPANIES ZAMBIA — V5 STABLE
One Group. Multiple Solutions.

This version is a stability patch for V5.
- Uses a local optimized logo file.
- Shows 04 Business Units and 01 Group Vision even if JavaScript is unavailable.
- JavaScript is defensive and loaded after page parsing.
- External phone images have a fallback if an image service is unavailable.
- Removed the Google Fonts network dependency so the page does not depend on that external service.
- Keeps MAX Mobile catalogue, brand filters, WhatsApp enquiries, MAX Apartments, and the blue/white/yellow branding.

Upload the contents of this package to the GitHub Pages repository root, replacing the old files.
