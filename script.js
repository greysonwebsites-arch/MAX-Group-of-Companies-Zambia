const menu=document.querySelector('.menu');const nav=document.querySelector('.nav');if(menu)menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const counters=document.querySelectorAll('.counter');function countUp(el){const target=Number(el.dataset.target||0);let n=0;const step=Math.max(1,Math.ceil(target/20));const t=setInterval(()=>{n+=step;if(n>=target){n=target;clearInterval(t)}el.textContent=n},45)}if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){countUp(e.target);io.unobserve(e.target)}}),{threshold:.6});counters.forEach(c=>io.observe(c))}else counters.forEach(countUp);
const cards=[...document.querySelectorAll('.product-card')],filters=[...document.querySelectorAll('.filter')],search=document.querySelector('#phoneSearch');let activeBrand='all';function applyFilters(){const q=(search?.value||'').toLowerCase();cards.forEach(card=>{const okBrand=activeBrand==='all'||card.dataset.brand===activeBrand;const okText=!q||card.innerText.toLowerCase().includes(q);card.style.display=okBrand&&okText?'':'none'})}filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(b=>b.classList.remove('active'));btn.classList.add('active');activeBrand=btn.dataset.brand;applyFilters()}));if(search)search.addEventListener('input',applyFilters);
